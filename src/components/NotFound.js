import React from "react";

//NotFound component for when no results are returned from a search query
function NotFound() {
  return <h1 className="center">Not Found</h1>;
}

export default NotFound;
